# Archive

Processed raw sources — fully ingested into `01 Reference/`. Read-only.

These files have been run through `ingest-source` and their knowledge extracted into reference articles. They are kept here for audit trail only — do not re-process.

To check what was created from a specific source, see `05 Iteration Logs/Ingestion Log.md`.
